# yaml-language-server: $schema=https://raw.githubusercontent.com/oapi-codegen/oapi-codegen/HEAD/configuration-schema.json
package: swallow_channel_from_service
output: ./gen_openapi/swallow_channel_from_service/client_openapi.gen.go
generate:
  models: true
  client: true # we use this client in the GATEWAY module, but openapi.yaml we give to an external client for implementation and the make request to them