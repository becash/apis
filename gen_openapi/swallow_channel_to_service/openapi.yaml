# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: ""
    version: 0.0.1
paths:
    /v1/product:
        post:
            tags:
                - ServiceFromSwallow
            operationId: ServiceFromSwallow_UpsertProduct
            parameters:
                - name: id
                  in: query
                  description: 'item ID, if filed exist: Update else Create'
                  schema:
                    type: integer
                    format: int32
                - name: jsonMetadata
                  in: query
                  description: |-
                    client side, data, now we not using them,
                     but in future if field will participate to internal logic need to be added to Product fields
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/swallow_channel_from_service.Product'
    /v1/product-availabilities/{productId}:
        get:
            tags:
                - ServiceToSwallow
            description: Get on product by some field
            operationId: ServiceToSwallow_GetAvailabilityOfProduct
            parameters:
                - name: productId
                  in: path
                  required: true
                  schema:
                    type: integer
                    format: int32
                - name: range.gte
                  in: query
                  schema:
                    type: string
                    format: date-time
                - name: range.lte
                  in: query
                  schema:
                    type: string
                    format: date-time
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/swallow_channel_to_service.Availabilities'
    /v1/product/{id}:
        get:
            tags:
                - ServiceFromSwallow
            description: Get on product by some field
            operationId: ServiceFromSwallow_GetProduct
            parameters:
                - name: id
                  in: path
                  description: Filter documents by ID Field
                  required: true
                  schema:
                    type: integer
                    format: int32
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/swallow_channel_from_service.Product'
    /v1/products:
        get:
            tags:
                - ServiceFromSwallow
            operationId: ServiceFromSwallow_GetProducts
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/swallow_channel_from_service.Product'
components:
    schemas:
        swallow_channel_from_service.Product:
            type: object
            properties:
                id:
                    type: integer
                    description: item ID
                    format: int32
                jsonMetadata:
                    type: string
                    description: |-
                        client side, data, now we not using them,
                         but in future if field will participate to internal logic need to be added to Product fields
            description: Can be Ticket/Reservation
        swallow_channel_to_service.Availabilities:
            type: object
            properties:
                data:
                    $ref: '#/components/schemas/swallow_channel_to_service.Availability'
                count:
                    type: string
        swallow_channel_to_service.Availability:
            type: object
            properties:
                productId:
                    type: integer
                    format: int32
                dateTime:
                    type: string
                    format: date-time
                currency:
                    type: integer
                    format: enum
tags:
    - name: ServiceFromSwallow
    - name: ServiceToSwallow
